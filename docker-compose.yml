version: '3'

services:
    service_backend:
      build:
        context: .
      ports:
        - "30010:3000"
      restarts: unless_stopped
      depends_on:
        - service_database

    service_database:
      image: mongo:5
      ports:
        - "30040:27017"
